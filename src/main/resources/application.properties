# ============================================================
# Devs Hair and Skin Clinic — Backend Configuration
# ============================================================

spring.application.name=devs-clinic-backend

# ------------------------------------------------------------
# Server — Render dynamically assigns PORT
# ------------------------------------------------------------
server.port=${PORT:8080}

# ------------------------------------------------------------
# MongoDB Configuration
# ------------------------------------------------------------
spring.data.mongodb.uri=mongodb+srv://clinic:clinic1234@clinic.go6wt7l.mongodb.net/devsclinic?retryWrites=true&w=majority&appName=clinic

# ------------------------------------------------------------
# Resend Email API
# ------------------------------------------------------------
resend.api.key=${RESEND_API_KEY}
resend.from.email=onboarding@resend.dev

# ------------------------------------------------------------
# Clinic-specific settings
# ------------------------------------------------------------
clinic.name=Devs Hair and Skin Clinic
clinic.notification.email=${CLINIC_NOTIFICATION_EMAIL:ssivakumar93095@gmail.com}

# ------------------------------------------------------------
# CORS
# ------------------------------------------------------------
cors.allowed-origins=http://localhost:3000,http://localhost:5173,https://clinic-frontend-phi.vercel.app,https://*.onrender.com

# ------------------------------------------------------------
# Validation / Jackson
# ------------------------------------------------------------
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.deserialization.fail-on-unknown-properties=false

# ------------------------------------------------------------
# Logging
# ------------------------------------------------------------
logging.level.root=INFO
logging.level.com.devsclinic.backend=DEBUG
logging.level.org.springframework.data.mongodb=INFO